import { VStack, Text, Center, Heading, ScrollView } from 'native-base';

export function Profile(){
  return(
    <Center flex={1} > 
      <Text color={"white"}>
        Profile
      </Text>
    </Center>
  )
}