import { VStack, Text, Center, Heading, ScrollView } from 'native-base';

export function Home(){
  return(
      <Center flex={1}> 
        <Text>
          Home
        </Text>
     </Center>
  );
} 