import { VStack, Text, Center, Heading, ScrollView } from 'native-base';

export function History(){
  return(
    <Center flex={1} > 
      <Text color={"white"}>
        History
      </Text>
    </Center>
  )
}