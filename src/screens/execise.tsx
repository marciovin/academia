import { VStack, Text, Center, Heading, ScrollView } from 'native-base';

export function Execise(){
  return(
    <Center flex={1} > 
      <Text color={"white"}>
        Execise
      </Text>
    </Center>
  )
}